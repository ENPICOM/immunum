% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polars.R
\name{polars_segmentation_method}
\alias{polars_segmentation_method}
\title{Segment sequences using a pre-built Annotator (polars)}
\usage{
polars_segmentation_method(expr, annotator)
}
\arguments{
\item{expr}{A polars expression or column name (string).}

\item{annotator}{A pre-built \link{Annotator} R6 instance.}
}
\value{
A \code{polars} expression returning a struct column with the same
schema as \code{\link[=polars_segment]{polars_segment()}}.
}
\description{
Polars-expression equivalent of \code{\link[=polars_segment]{polars_segment()}} that reuses an
\link{Annotator} instance built outside the call. Mirrors
\code{immunum.polars.segmentation_method()} from the Python wrapper.
}
\examples{
\dontrun{
library(polars)
ann <- Annotator$new(chains = c("H", "K", "L"), scheme = "imgt")
df <- pl$DataFrame(sequence = c("QVQLVQSGAEV...", "DIQMTQSPSSL..."))
df$with_columns(
  polars_segmentation_method("sequence", annotator = ann)$alias("segmented")
)
}
}
