[project]
name = "immunum2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anarci",
    "antpack==0.3.8.6",
    "biopython",
    "pandas",
    "mypy",
    "pandas-stubs",
    "matplotlib",
    "seaborn",
]

[tool.uv.sources]
anarci = { path = "data/anarci-1.3-py3-none-any.whl" }
