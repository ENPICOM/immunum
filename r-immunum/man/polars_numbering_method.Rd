% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polars.R
\name{polars_numbering_method}
\alias{polars_numbering_method}
\title{Number sequences using a pre-built Annotator (polars)}
\usage{
polars_numbering_method(expr, annotator)
}
\arguments{
\item{expr}{A polars expression or column name (string).}

\item{annotator}{A pre-built \link{Annotator} R6 instance.}
}
\value{
A \code{polars} expression returning a struct column with the same
schema as \code{\link[=polars_number]{polars_number()}}.
}
\description{
Polars-expression equivalent of \code{\link[=polars_number]{polars_number()}} that reuses an
\link{Annotator} instance built outside the call. Prefer this when running
the same annotator across multiple expressions or queries -- it skips
the per-batch annotator construction step. Mirrors
\code{immunum.polars.numbering_method()} from the Python wrapper.
}
\examples{
\dontrun{
library(polars)
ann <- Annotator$new(chains = c("H", "K", "L"), scheme = "imgt")
df <- pl$DataFrame(sequence = c("QVQLVQSGAEV...", "DIQMTQSPSSL..."))
df$with_columns(
  polars_numbering_method("sequence", annotator = ann)$alias("numbered")
)
}
}
