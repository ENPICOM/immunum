% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polars.R
\name{polars_number}
\alias{polars_number}
\title{Number sequences inside a polars expression}
\usage{
polars_number(expr, chains, scheme, min_confidence = NULL)
}
\arguments{
\item{expr}{A polars expression or column name (string). When a
string, it is parsed as \code{pl$col(expr)}.}

\item{chains}{Character vector of chain identifiers. Same alias set
as \link{Annotator}\verb{$new()} (case-insensitive: H/K/L/A/B/D/G,
heavy/kappa/lambda/..., IGH/IGK/...).}

\item{scheme}{\code{"IMGT"} or \code{"Kabat"}, case-insensitive.}

\item{min_confidence}{Optional numeric in \verb{[0, 1]}. \code{NULL} (the
default) uses the Rust core's default of 0.5.}
}
\value{
A \code{polars} expression returning a struct column.
}
\description{
Vectorized polars expression that runs immunum's numbering on each
element of an input string column. Mirrors \code{immunum.polars.number()}
from the Python wrapper. Returns a polars expression of struct type
with fields \code{chain}, \code{scheme}, \code{confidence}, \code{positions}, \code{residues}.
}
\details{
Failed sequences (NA, alignment failure, sub-threshold confidence)
produce a row of polars nulls. The expression always preserves
input row count.
}
\examples{
\dontrun{
library(polars)
df <- pl$DataFrame(sequence = c("QVQLVQSGAEV...", "DIQMTQSPSSL..."))
df$with_columns(
  polars_number("sequence", chains = c("H", "K", "L"), scheme = "imgt")$
    alias("numbered")
)
}
}
